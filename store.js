import { atom } from "jotai";

// Atom for favourites (you likely already have this)
export const favouritesAtom = atom([]);

// Atom for search history (YOU ARE MISSING THIS)
export const searchHistoryAtom = atom([]);